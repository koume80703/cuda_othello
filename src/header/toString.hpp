#pragma once

#include <iostream>
#include <vector>
#include "enum.hpp"

using namespace std;

void print_vector_2d(vector< vector<BOARD_STATE> > v);
void print_vector(vector<BOARD_STATE> v);
void print_vector_pair(vector< pair<int, int> > v);
void print_pair(pair<int,int> p);
